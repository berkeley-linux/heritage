# $Id$

include $(TOPDIR)/Config.mk

.PHONY: all install clean
.SUFFIXES: .c .o

@output@: @objects@
	$(CC) $(LDFLAGS) -o $@ @objects@ $(LIBS) @ifreq@

.c.o:
	$(CC) $(CFLAGS) -c -o $@ $<

install: @output@
	mkdir -p $(PREFIX)/bin/
	cp @output@ $(PREFIX)/bin/

clean:
	rm -f *.o @output@
